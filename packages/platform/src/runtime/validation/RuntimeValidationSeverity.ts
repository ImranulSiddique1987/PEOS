/**
 * PEOS™
 * Runtime Validation Severity
 *
 * Defines the severity levels used by the runtime validation subsystem.
 */

export enum RuntimeValidationSeverity {
  Info = "info",
  Warning = "warning",
  Error = "error",
  Critical = "critical",
}
